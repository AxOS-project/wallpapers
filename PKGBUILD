pkgname="axos-wallpaper-collection"
pkgver="1.1"
pkgrel="1"
pkgdesc="axos wallpaper collection"
arch=("x86_64")


package(){
   mkdir -p ${pkgdir}/usr/share/
   cp -r ${srcdir}/backgrounds/ ${pkgdir}/usr/share/

}

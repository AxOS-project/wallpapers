pkgname="axos-wallpaper-collection"
pkgver="1.0"
pkgrel="1"
pkgdesc="axos wallpaper collection"
arch=("x86_64")


package(){
   mkdir -p ${pkgdir}/etc/skel/
   cp -r ${srcdir}/wallpaper/ ${pkgdir}/etc/skel/

}
